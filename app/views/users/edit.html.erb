<%= form_for :user, url: user_path(@user), method: :patch do |user_form| %>
  <%= user_form.hidden_field(:id)%>
  <% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@user.errors.count, "error") %> prohibited
      this user from being saved:</h2>
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  
  <% @user.bets.each_with_index do |bet, index| %>    
    <%= user_form.fields_for "bets_attributes[#{index + 1}]", bet do |bets_form| %>
    <div>
       <p> 
          <%= bets_form.hidden_field(:game_id, value: bet.game.id)%>
          <%= bets_form.label bet.game.team1.name %>
          <%= bets_form.text_field :score_team1 %>          
          <%= bets_form.label bet.game.team2.name %>
          <%= bets_form.text_field :score_team2 %>
        </p>
    <% end %>   
  <% end %>   
  <p>
    <%= user_form.submit %>
  </p>
<% end %>
