<table class="table">
	<thead>
    <tr>
      <th>#</th>
      <th>Jogo</th>
      <th>Preenchido</th>
    </tr>
  </thead>
  <tbody>    
    	<% @games.each do |game| %>
	    	<tr>
					<td><%= game.number %></td>
					<td><%= game.done?? game.label : link_to(game.label, edit_game_path(game)) %></td>
					<td><%= game.done?? "OK" : "-" %></td>
				</tr>
			<% end %>    
  </tbody>
	
	
</table>